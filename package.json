{
  "name": "sloparena-workspace",
  "private": true,
  "version": "0.1.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"npm run dev -w sloparena-api\" \"npm run dev -w sloparena-web\"",
    "dev:api": "npm run dev -w sloparena-api",
    "dev:web": "npm run dev -w sloparena-web",
    "join": "node packages/cli/dist/index.js go --server http://localhost:4000 --web http://localhost:5173",
    "build": "npm run build -w @sloparena/shared && npm run build -w sloparena && npm run build -w sloparena-api && npm run build -w sloparena-web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
