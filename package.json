{
  "name": "usageboard-workspace",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"npm run dev -w usageboard-api\" \"npm run dev -w usageboard-web\"",
    "dev:api": "npm run dev -w usageboard-api",
    "dev:web": "npm run dev -w usageboard-web",
    "join": "node packages/cli/dist/index.js go --server http://localhost:4000 --web http://localhost:5173",
    "build": "npm run build -w @usageboard/shared && npm run build -w sloparena && npm run build -w usageboard-api && npm run build -w usageboard-web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
